{
  "name": "fitcheck",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI-powered OOTD feedback for different occasions",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
